Run linter for the current project.

Detect the project type and run appropriate lint command:
- Node.js: `npm run lint`
- Go: `golangci-lint run`
- Rust: `cargo clippy`
- Python: `ruff check .` or `flake8`

Report any issues with suggestions for fixes.
