Run tests for the current project.

Detect the project type and run appropriate test command:
- Node.js: `npm test`
- Go: `go test ./...`
- Rust: `cargo test`
- Python: `pytest`

Report any failures with suggestions for fixes.
