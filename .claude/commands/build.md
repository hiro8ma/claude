Build the current project.

Detect the project type and run appropriate build command:
- Node.js: `npm run build`
- Go: `go build ./...`
- Rust: `cargo build`
- TypeScript: `tsc`

Report any build errors with suggestions for fixes.
