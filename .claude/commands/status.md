Show the current git status.

1. Run `git status` to see working tree status
2. Run `git branch` to show current branch
3. Summarize:
   - Staged changes
   - Unstaged changes
   - Untracked files
   - Commits ahead/behind remote
