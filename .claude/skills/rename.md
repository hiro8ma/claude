Rename across the codebase: $ARGUMENTS

1. Parse the old and new names from the arguments
2. Find all occurrences of the old name
3. Replace with the new name
4. Update imports and references
5. Run tests to verify no breakage
